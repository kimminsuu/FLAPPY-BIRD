# Daily Work Log - 2026-02-24

## 작업 내용

### GitHub Actions CI/CD 구축
- `.github/workflows/ci.yml` 생성 (dev, main 브랜치 push + PR 트리거)
- 파이프라인: `npm ci` → `npm run lint` → `npm run build`
- CI 실패 디버깅 및 수정:
  - `next lint` 명령 제거 (Next.js 16에서 삭제됨) → ESLint 직접 실행으로 전환
  - `createClient("")` 에러 수정 (env vars 없을 때 null fallback 처리)

### ESLint 설정 추가
- `eslint`, `eslint-config-next` 설치
- `eslint.config.mjs` 생성 (ESLint 9 flat config, `next/core-web-vitals` 확장)
- `android/`, `.next/` 디렉토리 lint 제외
- `react-hooks/set-state-in-effect` 규칙 warn으로 완화 (v7 신규 규칙)
- `package.json` lint 스크립트 변경: `next lint` → `eslint .`
- CI에 lint 단계 복원

### 비용 산정 문서 작성
- `docs/cost_estimation.md` 생성
- 서비스별 프리 티어 한도, 사용자 규모별 월간 비용, 모바일 배포 비용 정리

### 문서 업데이트
- CLAUDE.md: CI/CD 섹션 추가, Project Structure에 eslint.config.mjs 추가

## 수정된 파일
- `.github/workflows/ci.yml` — 신규 생성
- `eslint.config.mjs` — 신규 생성
- `lib/supabase.ts` — env vars 없을 때 null fallback
- `package.json` — eslint 설치, lint 스크립트 변경
- `package-lock.json` — eslint 의존성 추가
- `CLAUDE.md` — CI/CD 섹션 추가
- `docs/cost_estimation.md` — 신규 생성
- `docs/daily_work/2026-02-24.md` — 신규 생성

## 다음 작업
- [ ] Phase 4: 모바일 앱 패키징 (Capacitor)
- [ ] Stage 9~ 추가 기믹 설계 및 구현
