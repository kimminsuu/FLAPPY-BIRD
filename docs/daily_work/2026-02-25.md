# Daily Work Log - 2026-02-25

## 작업 내용

### 일일 보상 (Daily Reward) 기능 구현
7일 주기 일일 보상 시스템 전체 구현. 홈 화면 진입 시 미수령 보상 자동 팝업.

**보상 테이블 (7일 주기 순환):**
| Day | 보상 |
|-----|------|
| 1 | 코인 200 |
| 2 | 코인 300 |
| 3 | 레어 새 (미보유 랜덤) |
| 4 | 코인 500 |
| 5 | 에픽 새 (미보유 랜덤) |
| 6 | 코인 700 |
| 7 | 유니크 새 (미보유 랜덤) |

**구현 내역:**
- DB 스키마: USER 테이블에 `reward_day`, `last_reward_date` 컬럼 추가 (ALTER TABLE)
- 보상 로직 모듈: KST 자정 기준 날짜 계산, 등급별 미보유 새 랜덤 선택, 대체 코인 처리
- 서비스 함수: `claimDailyReward()` — 보상 수령 전체 플로우 (조회→검증→지급→업데이트)
- 모달 UI: 7일 캘린더 그리드, 코인 카운트업 애니메이션, 새 카드 플립 연출, 반짝이 파티클
- 홈 화면 연동: 마운트 시 자동 팝업 + "일일보상" 버튼으로 수동 확인 가능

**버그 수정:**
- `getDayStatus` 로직 오류 수정 — `day < todayDay` 대신 `day <= currentDay`로 변경하여 실제 수령 완료 일차만 체크 표시

### 게임 설명 모달 업데이트
- 홈 화면 (i) 모달에 일일 보상 섹션 추가 (7일 보상표 + 코인 대체 안내)

## 수정된 파일
- `lib/daily-rewards.ts` — **신규 생성** (보상 정의, KST 날짜 계산, 새 랜덤 선택)
- `components/daily-reward-modal.tsx` — **신규 생성** (7일 캘린더 모달 + 연출)
- `lib/user-service.ts` — DbUser 타입 확장, `claimDailyReward()` 함수 추가
- `components/home-page.tsx` — 일일 보상 모달 연동, "일일보상" 버튼 추가, 설명 모달 업데이트
- `app/globals.css` — shimmer, sparkle 키프레임 애니메이션 추가
- `supabase/migration.sql` — ALTER TABLE 문 추가 (reward_day, last_reward_date)
- `docs/er_diagram.md` — USER 엔터티에 새 컬럼 반영
- `CLAUDE.md` — Project Structure에 신규 파일 추가

## 다음 작업
- [ ] 게임 소리 추가 (효과음/BGM)
- [ ] Stage 9~ 추가 기믹 설계 및 구현
