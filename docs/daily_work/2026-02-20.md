# Daily Work Log - 2026-02-20

## 작업 내용

### Supabase DB 구축 (배포 Phase 1)
- Supabase 프로젝트 생성
- ERD 수정: USER 테이블에서 password 제거, device_id 추가
- 테이블 생성: user, bird, user_bird, game_score, user_stage_record
- BIRD 마스터 데이터 시드 (37마리)
- RLS 정책 설정
- `supabase/migration.sql` 마이그레이션 파일 생성

### 프론트엔드 DB 연동 (배포 Phase 2)
- `@supabase/supabase-js` 설치 + 클라이언트 초기화 (`lib/supabase.ts`)
- 기기 UUID 유틸 (`lib/device.ts`) — localStorage에 영구 보관
- UserProvider + useUser hook (`lib/user-context.tsx`)
- Supabase CRUD 서비스 (`lib/user-service.ts`)
  - 유저 조회/생성, 닉네임 변경, 코인 관리
  - 새 보유/장착, 점수 갱신, 스테이지 기록
- 모든 화면 localStorage → Supabase DB 전환 완료
  - LoginPage: createUser(), isUsernameAvailable()
  - HomePage: updateUsername(), patchUser()
  - BirdSelectionPage: getOwnedBirdIds(), getCoins(), subtractCoins(), addCoins(), equipBird(), addBird()
  - StageSelectPage: getStageRecords()
  - GamePage: addCoins(), updateHighScore(), updateStageBest()
- 계절 테마 DB 저장 (`season-context.tsx` → updateSeason() 연동)

### 문서 업데이트
- CLAUDE.md 전면 수정 (Tech Stack, TODO, Phase 계획, Project Structure)
- ERD 문서 업데이트 (device_id 추가, password 제거, 실제 DB 컬럼명 반영)
- features.md 업데이트 (DB 연동 상태, Stage 7-8, 가챠 UI)
- daily_work 로그 누락분 보충 (2026-02-06 ~ 2026-02-19)

## 수정된 파일
- `lib/supabase.ts` — 신규 생성
- `lib/device.ts` — 신규 생성
- `lib/user-context.tsx` — 신규 생성
- `lib/user-service.ts` — 신규 생성
- `lib/season-context.tsx` — DB 저장 연동
- `app/providers.tsx` — UserProvider 추가
- `components/login-page.tsx` — Supabase 유저 생성
- `components/home-page.tsx` — Supabase 닉네임 변경
- `components/bird-selection-page.tsx` — Supabase 코인/새 관리
- `components/stage-select-page.tsx` — Supabase 스테이지 기록
- `components/game-page.tsx` — Supabase 점수/코인 저장
- `components/ui/UserInfoBar.tsx` — useUser() Context 사용
- `package.json` — @supabase/supabase-js 추가
- `supabase/migration.sql` — 신규 생성
- `CLAUDE.md` — 전면 수정
- `docs/er_diagram.md` — device_id 반영
- `docs/features.md` — DB 연동 반영
- `docs/daily_work/` — 누락분 보충

## 다음 작업
- [ ] Phase 3: Vercel 웹 배포
